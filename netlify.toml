[build]
  base = "/"  # Explicitly set the base directory to root
  command = "pnpm run build"
  publish = "dist"

# Handle SPA routing for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200